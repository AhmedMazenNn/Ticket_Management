/**
 * Shared TypeScript types and interfaces.
 *
 * All domain types are defined here and imported across the app.
 * Do NOT define types inline in components.
 *
 * Sub-modules (added per feature):
 *   $lib/types/ticket.ts
 *   $lib/types/user.ts
 *   $lib/types/api.ts      — generic API response envelopes
 *   $lib/types/dashboard.ts
 */
