/**
 * Utility functions.
 *
 * Pure, side-effect-free helpers used across the app:
 *   formatDate, formatRelativeTime
 *   cn (class-name merging)
 *   truncate, capitalize
 *   priorityColor, statusColor
 */
