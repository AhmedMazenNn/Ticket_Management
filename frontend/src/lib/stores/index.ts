/**
 * Svelte stores.
 *
 * Global reactive state lives here:
 *   authStore   — current user + JWT token
 *   ticketStore — cached ticket list
 *   uiStore     — loading states, toasts, sidebar open/closed
 *
 * Sub-modules are created alongside their feature branch.
 */
