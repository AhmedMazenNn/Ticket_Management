/**
 * Re-exports barrel for $lib.
 *
 * Import from '$lib' to get the public API surface.
 * Each sub-module adds its own exports here as it is built.
 */

export { API_BASE } from './api/index.js';
